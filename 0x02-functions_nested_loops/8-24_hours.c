#include "main.h"
/**
 *Jack Bauer : Starting from 00:00 to 23:59
 */
void jack_bauer(void)
{
	int a;
	int b;
	for (a == 00; a < 24; a++)
	{
		for (b == 00; b <= 59; b++)
			printf("%d : %d", a, b);
	}
}
