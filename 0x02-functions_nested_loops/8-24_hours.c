#include "main.h"
#include <stdio.h>
/**
 *Jack Bauer : Starting from 00:00 to 23:59
 */
void jack_bauer(void)
{
	int a;
	int b;
	for (a == 0; a < 24; a++)
	{
		for (b == 0; b <= 59; b++)
			printf("%d : %d", a, b);
	}
}
